# Quiz 08 - Decorators

1. What is a Decorator?
2. What does `@decorator` mean?
3. Why do we use `functools.wraps`?
4. Why are `*args` and `**kwargs` useful inside decorators?
5. Give one Data Engineering use case.
6. Give one AI/ML use case.
7. What does a timer decorator measure?