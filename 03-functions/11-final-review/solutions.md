# Master Quiz Answers

Q1 → Using `def`

Q2 → Variable defined in a function definition.

Q3 → Value passed to a function.

Q4 → Sends a value back and exits the function.

Q5 → *args = positional arguments.
**kwargs = keyword arguments.

Q6 → Tuple.

Q7 → Dictionary.

Q8 → Local → Enclosing → Global → Built-in.

Q9 → Anonymous function.

Q10 → Applies a function to each element.

Q11 → Filters elements based on a condition.

Q12 → Reduces multiple values into one result.

Q13 → A function calling itself.

Q14 → Condition that stops recursion.

Q15 → Function that accepts or returns another function.

Q16 → Inner function remembering enclosing variables.

Q17 → Function that modifies/extends another function.

Q18 → Produces a value and pauses execution.

Q19 → Object that produces values one at a time.

Q20 → Creates an iterator.

Q21 → Returns the next value.

Q22 → StopIteration.

Q23 → They process values one at a time.

Q24 →

```python
def calculate_profit(sales, cost):
    return sales - cost

Q25 →

def calculate_margin(sales, cost):
    return (sales - cost) / sales * 100

Q26 →

def total_sales(*sales):
    return sum(sales)

Q27 →

[x for x in numbers if x % 2 == 0]

Q28 →

def numbers():
    for i in range(1, 11):
        yield i

Q29 →

Meaningful names
Single responsibility
Avoid global variables
Use type hints
Use docstrings

Q30 →

Functions make Data Analysis code reusable,
testable, maintainable, and easier to build into
ETL, automation, ML and AI pipelines.