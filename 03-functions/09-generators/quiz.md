# Quiz 09 - Generators

1. What is a Generator?
2. What does `yield` do?
3. What is the main advantage of Generators?
4. What is the difference between `return` and `yield`?
5. What is the output?

```python
def test():
    yield 10
    yield 20
    yield 30

print(list(test()))
Why are Generators useful for large datasets?
Give one ETL use case.
What is a Generator Expression?