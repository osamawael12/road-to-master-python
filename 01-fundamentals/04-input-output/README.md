# 04 - Input & Output

## Objectives

- Use `print()`
- Use `input()`
- Convert user input
- Use `int()`
- Use `float()`
- Build interactive programs

## Important

`input()` always returns a string.

```python
age = input("Age: ")
age = int(age)