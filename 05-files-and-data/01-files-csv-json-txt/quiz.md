# Quiz 01 - Files & Data

1. Why do we use `with open()`?
2. What does `"r"` mean?
3. What does `"w"` mean?
4. What does `"a"` mean?
5. What is CSV?
6. What is JSON?
7. Which module handles CSV?
8. Which module handles JSON?
9. What does `json.load()` do?
10. What does `json.dump()` do?
11. Why is JSON important for APIs?
12. Give one Data Analysis use case for CSV.